import React, { Component, ReactNode } from "react";

export default class Plane extends Component {

    constructor(props) {
        super(props);
    }

    render(): ReactNode {
        return (
            <div id="plane">
            </div>
        );
    }
}